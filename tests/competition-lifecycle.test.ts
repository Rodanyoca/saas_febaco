import assert from "node:assert/strict";import test from "node:test";import { requireMutableEdition } from "../lib/competition-lifecycle";import { CompetitionParticipationError } from "../lib/competition-participants";
test("autorise les mutations d’une édition active",()=>{assert.equal(requireMutableEdition([{id_competition:"C1",statut:"EN_COURS"}],"C1").statut,"EN_COURS")});
test("refuse les mutations ordinaires d’une édition clôturée",()=>{assert.throws(()=>requireMutableEdition([{id_competition:"C1",statut:"TERMINEE"}],"C1"),(e:unknown)=>e instanceof CompetitionParticipationError&&e.code==="EDITION_CLOTUREE")});
test("la clôture exige une session fédérale et conserve l’identifiant de route",async()=>{const source=await import("node:fs/promises").then(fs=>fs.readFile("app/api/competitions/[id]/close/route.ts","utf8"));assert.match(source,/user\.role!=="federal"/);assert.match(source,/idHeader:"id_competition",id,mode:"update"/);assert.match(source,/statut:"TERMINEE"/)});
