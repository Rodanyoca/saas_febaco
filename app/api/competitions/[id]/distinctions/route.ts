import { NextResponse } from "next/server";
import { getSessionUser } from "@/lib/auth-session";
import { CompetitionParticipationError } from "@/lib/competition-participants";
import { createCompetitionDistinction, getCompetitionDistinctions } from "@/lib/competition-distinctions";
const fail=(error:unknown)=>error instanceof CompetitionParticipationError?NextResponse.json({error:{code:error.code,message:error.message,fields:error.fields}},{status:error.status}):NextResponse.json({error:{code:"SERVICE_INDISPONIBLE",message:"Service temporairement indisponible."}},{status:500});
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){if(!(await getSessionUser()))return NextResponse.json({error:{message:"Authentification requise."}},{status:401});try{return NextResponse.json(await getCompetitionDistinctions(decodeURIComponent((await params).id)))}catch(error){return fail(error)}}
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const user=await getSessionUser();if(!user)return NextResponse.json({error:{message:"Authentification requise."}},{status:401});if(user.role!=="federal")return NextResponse.json({error:{message:"Droit fédéral requis."}},{status:403});try{const id=decodeURIComponent((await params).id),created=await createCompetitionDistinction(id,await request.json().catch(()=>null));return NextResponse.json({created,...await getCompetitionDistinctions(id)},{status:201})}catch(error){return fail(error)}}
